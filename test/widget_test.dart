// Basic smoke test. Run geometry and routing tests with:
//   flutter test test/geometry test/routing
// (Full app test may hit dependency issues with street_view_platform_interface.)

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
